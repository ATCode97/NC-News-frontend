import React, { Component } from "react";

//1. need error handling for 404 for an none existence path
//2. 400 error if reach an invalid article_id
//3. not allow post if not fill all boxes <--- require on the  comment box

const ErrorPage = () => {
  return (
    <div>
      <h2>Error Error Error</h2>
    </div>
  );
};

export default ErrorPage;
